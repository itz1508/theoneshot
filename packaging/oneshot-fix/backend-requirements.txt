# Intentional Docker-compatible subset of audisor_backend/uv.lock; NOT a
# direct unfiltered `uv export` output. A full export also emits the editable
# runtime line (-e ../openai_project/runtime) and the runtime's dependency
# closure; packaging/oneshot-fix/Dockerfile installs those earlier via
# runtime-requirements.txt and the runtime package install. This file pins
# only the backend's remaining direct closure (pydantic and its dependencies).
# Keep these pins in sync with audisor_backend/uv.lock.
annotated-types==0.7.0
pydantic==2.13.4
pydantic-core==2.46.4
typing-extensions==4.16.0
typing-inspection==0.4.2
