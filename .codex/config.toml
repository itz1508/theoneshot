# Workspace-level Codex configuration.
# A-Flow is the repository's custom analysis agent and is defined as a
# standalone project agent under .codex/agents.
[agents.aflow]
config_file = "./agents/aflow.toml"

[features]
hooks = true
